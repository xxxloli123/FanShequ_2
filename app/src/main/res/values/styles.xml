<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/skyblue</item>
        <item name="colorPrimaryDark">@color/skyblue</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue2</item>
        <item name="colorPrimaryDark">@color/blue2</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="AppTheme.Main" parent="AppTheme" >
        <item name="colorPrimary">@color/skyblue</item>
        <item name="colorPrimaryDark">@color/skyblue</item>
    </style>

    <style name="home_tab_button">
        <item name="android:textSize">13sp</item>
        <item name="android:textColor">@drawable/home_tab_selctor</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:padding">3dp</item>
    </style>
    <!-- 选择照片底部弹出层 -->
    <style name="PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <style name="my_checkbox_selector" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/setdefault_checkbox_selector</item>
    </style>

    <style name="myProgressBarStyleLarge">
        　　
        <item name="android:indeterminateDrawable">@drawable/progress_large_white</item>
    </style>

    <style name="TabRadioButtonNoText">
        <item name="android:button">@null</item>
        <item name="android:background">@null</item>
    </style>

    //继承兼容包的Button样式，就会有悬浮效果了
    <style name="AppTheme.Button" parent="Widget.AppCompat.Button">
        <item name="android:button">@null</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/blue2</item>
    </style>
    <style name="CheckBoxGreen" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/green</item>
    </style>
</resources>
